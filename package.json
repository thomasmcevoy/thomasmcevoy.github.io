{
  "name": "thomas-mcevoy.com",
  "version": "2.4.0",
  "description": "Thomas McEvoy official website",
  "author": "Thomas McEvoy",
  "license": "ISC",
  "scripts": {
    "start": "webpack --mode=development --watch",
    "build": "rm -rf dist/* && webpack --mode=production && sass src/styles/index.scss dist/main.css && cp -rf static/* dist && cp src/index.html dist"
  },
  "dependencies": {
    "koa": ">=2.5.3",
    "koa-static": "^5.0.0",
    "webpack": ">=4.20.2",
    "webpack-cli": "^3.1.2"
  }
}
